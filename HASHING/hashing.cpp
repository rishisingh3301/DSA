#include<bits/stdc++.h>
using namespace std;

int main()
{
    // //number hashing
    // int arr[]={1,5,4,2,1,5,6,3,7};
    // int n;cin>>n;
    // //preparing hash table
    // int hash[10]={0};
    // for (int i = 0; i < 9; i++)
    // {
    //     hash[arr[i]]+=1;
    // }
    
    // while(n--){
    //     int num;cin>>num;
    //     //fetching
    //     cout<<"count of "<<num<<" is "<<hash[num]<<endl;
    // }

    // //characters hashing
    // string s;cin>>s;
    // int hash[26]={0};
    // // int hash[256]={0};
    // //precompute
    // for (int i = 0; i < s.size(); i++)
    // {
    //     hash[s[i]-'a']+=1;
    // }
    // int n;cin>>n;
    // while(n--){
    //     char c;cin>>c;
    //    cout<<"count of "<<c<<" is "<<hash[c-'a']<<endl;    }
    
    return 0;
}